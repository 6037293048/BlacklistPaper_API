services:
  - type: web
    name: blacklist-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node src/index.js
    autoDeploy: true
    envVars:
      - key: API_KEY
        sync: false
      - key: DATABASE_PATH
        value: /data/blacklist.db
